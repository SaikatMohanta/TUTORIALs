# Example of lambda function with map()
numbers = [1, 2, 3, 4, 5]

# Square each number using lambda function and map()
squared_numbers = list(map(lambda x: x**2, numbers))

# Print the squared numbers
print("Squared numbers:", squared_numbers)

