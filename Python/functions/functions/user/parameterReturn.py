# User-defined function with a parameter and returns a value
def add_numbers(a, b):
    return a + b

# Calling the function and storing the returned value
result = add_numbers(3, 5)

# Printing the returned value
print("The sum is:", result)

